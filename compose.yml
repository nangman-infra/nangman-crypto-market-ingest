# Legacy local compose file.
#
# Do not use this file for deployment or runtime verification. The current
# market-ingest runtime is deployed through AWS ECS task definitions and IAM
# access to AWS S3.

name: nangman-crypto-market-ingest-legacy-compose-disabled

services: {}
