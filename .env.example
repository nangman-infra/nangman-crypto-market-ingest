# Legacy local env example.
#
# Do not use this file for deployment or runtime verification. The current
# market-ingest runtime configuration is owned by ECS task definitions, IAM,
# and AWS S3 bucket settings.
